# Frontend

Frontend web responsivo del catálogo de ferretería. No requiere instalar
dependencias: abre `index.html` con un servidor local, por ejemplo:

```bash
python -m http.server 5500
```

La aplicación consume la API Django en `http://127.0.0.1:8000/api`.
